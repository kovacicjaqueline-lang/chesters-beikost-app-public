{
  "name": "chesters-beikost-app",
  "version": "10.1.25",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check:deploy": "wrangler deploy --dry-run",
    "test": "node --test tests/*.test.js tests/*.test.cjs",
    "test:browser": "node browser-tests/ui-meal-editor-webkit.test.mjs && node browser-tests/planned-recipe-details-webkit.test.mjs && node browser-tests/unified-food-log-webkit.test.mjs"
  },
  "devDependencies": {
    "playwright": "^1.55.0",
    "wrangler": "^4.102.0"
  }
}
